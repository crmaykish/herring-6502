MEMORY {
    ZP:
        start = $0,
        size = $100,
        type = rw, 
        define = yes;

    RAM:
        start = $200,
        size = $E00,
        define = yes;
}

SEGMENTS {
    ZEROPAGE:
        load = ZP,
        type = zp,
        define = yes;
    STARTUP:
        load = RAM,
        type = ro;  
    
    CODE:
        load = RAM,
        type = ro; 
}
